# Quality Eval Suite

The offline evaluation suite (`gmdgen.eval.suite`) asserts that generation output meets minimal quality standards.

To run tests:
```powershell
python -m pytest -q tests\test_eval_suite.py
```
